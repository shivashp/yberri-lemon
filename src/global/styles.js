const colors = {
  primary: '#FFFFFF',
  secondary: '#F3F9FE',
  textPrimary: '#6C6C6C',
  textSecondary: '#7FBEFF',
};

const font = {
  large: 16,
  regular: 14,
  small: 12,
  family: '',
};

const styles = {
  colors, font,
};

export default styles;
