const colors = {
  primary: '#FFFFFF',
  secondary: '#F3F9FE',
  textPrimary: '#6C6C6C',
  textSecondary: '#7FBEFF',
};

const font = {
  large: 16,
  regular: 14,
  small: 12,
  family: '',
};

const container = {
  flex: 1,
  paddingHorizontal: 10,
  paddingVertical: 10,
};
const noPadding = {
  paddingHorizontal: 0,
  paddingVertical: 0,
};

const center = {
  alignItems: 'center',
  justifyContent: 'center',
};

const layout = {
  container, center, noPadding,
};

export {
  layout, colors, font,
};
